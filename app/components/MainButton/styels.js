import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    button: {
        width: '90%',
        height: 40,
        padding: 10,
        backgroundColor: '#4CAF50',
        borderRadius: 8,
        marginTop: 50,
        marginBottom: 150,
    },
    buttonText: {
        color: '#fff',
        textAlign: 'center',
        fontSize: 17,
        fontWeight: 'bold',
    }
});
